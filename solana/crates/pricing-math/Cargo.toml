[package]
name = "pricing-math"
version = "0.1.0"
edition = "2021"
description = "Fixed-price purchase/redemption quoting, ported 1:1 from FixedPriceStrategy.sol"
license = "MIT"

[lib]
crate-type = ["lib"]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
